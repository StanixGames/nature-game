import React, { useEffect } from 'react';
import {ObjectInfoSection} from './ObjectInfoSection';

export function GUI() {
  useEffect(() => {
    //
  }, []);
  
  return (
    <React.Fragment>
      <ObjectInfoSection />
    </React.Fragment>
  );
}
