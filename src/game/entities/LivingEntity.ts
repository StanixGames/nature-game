import Entity from './Entity';

interface LivingEntity extends Entity {
  // protected hp: number;

  // constructor(hp: number, x: number, y: number, width: number, height: number) {
  //   // super(x, y, width, height);
  //   this.hp = hp;
  // }
}

export default LivingEntity;
